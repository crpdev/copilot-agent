{
  "name": "math-chat-agent",
  "displayName": "Math Chat Agent",
  "description": "A GitHub Copilot agent for math operations using VS Code's GPT-4 and a MCP server.",
  "version": "1.0.0",
  "publisher": "math-agent",
  "engines": {
    "vscode": "^1.78.0"
  },
  "main": "extension.js",
  "activationEvents": [
  ],
  "contributes": {
    "chatParticipants": [
      {
        "id": "my-agent.math",
        "fullName": "Math Agent",
        "name": "math-agent",
        "description": "A GitHub Copilot agent for math operations using VS Code's GPT-4 and a MCP server.",
        "isSticky": true,
        "commands": [
          {
            "name": "math",
            "description": "Math operations"
          }
        ]
      }
    ]
  },
  "dependencies": {
    
  },
  "devDependencies": {
    "@types/node": "^16.0.0",
    "vscode": "^1.78.0"
  }
} 